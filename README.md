An old program used to read MFRSR data (see doc for more info)

# Installation
First, check i one of the past builds works on your machine.
If not:

## build from source
Remove old builds

```make clean 2>/dev/null || true```

Then build it 

```make```
